{% extends 'base.html.twig' %}

{% block body %}




    <div class="jumbotron">
        <h1 class=" text-center" >Ajout d'une catégorie</h1>
        <div>
            <a href="{{ path('show_category') }}">Listes des catégories</a>
            <br>
            <a href="{{ path('app_index') }}">Retour à la page d'accueil </a>
        </div>
    </div>




    <div class="form-inline justify-content-center">
        <div class="form-group mb-2">{{ form_start(form) }}</div>
        <div class="form-group col-2 mb-2">{{ form_row(form.name) }}</div>
        <button class="btn btn-primary mt-3">{{ button_label|default('Save') }}</button>
    </div>{{ form_end(form) }}


{% endblock %}